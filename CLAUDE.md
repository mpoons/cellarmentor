# CellarMentor, instructies voor Claude-sessies

Persoonlijke wijnkelder-PWA van Max. Live: **https://cellarmentor.com/** (GitHub Pages uit deze repo, `mpoons/cellarmentor`, eigen domein via `CNAME`; `mpoons.github.io/cellarmentor/` stuurt door). Brug op het oude adres: **https://mpoons.github.io/caveau/** (repo `mpoons/caveau`, gebouwd met `./brug.sh`): dezelfde app met verhuisbanner, zodat kelders die daar lokaal staan bereikbaar blijven. Alle communicatie en UI in het **Nederlands**.

## §0 Projectfeiten (de algemene werkregels staan in `~/.claude/CLAUDE.md`)
- **Naam:** CellarMentor sinds 15 sep 2026 (daarvoor Caveau). Bewust nog `caveau`: de opslagsleutels op het apparaat (`caveau_v1`, `caveau_backup_*`, IndexedDB `caveau_photos`, `caveau_cam`), de Supabase-secrets `CAVEAU_ANTHROPIC_KEY` en `CAVEAU_APP_URL`, en de pg_cron-jobs `caveau-herinnering` en `caveau-kosten`. Sleutels nooit hernoemen zonder migratie (data van gebruikers); secrets en cron-namen pas bij de sleutelvervanging van december 2026. Back-ups met `app:'caveau'` blijven importeerbaar.
- **Stack:** één HTML-bestand (`cellarmentor.html`: CSS, markup en vanilla JS in zes scriptblokken), geen framework, geen bundler, geen npm. Data lokaal (localStorage + IndexedDB). Server: Supabase (Postgres met RLS, Auth, Edge Functions in Deno/TypeScript, pg_cron). AI: Anthropic API. Betalen: Stripe (testmodus). Mail: Resend. Hosting: GitHub Pages uit `main` van `mpoons/cellarmentor`.
- **Lokaal draaien:** `cellarmentor.html` in de browser openen (file:// werkt), of `./build.sh` en `index.html` openen.
- **Tests:** `tests/cellarmentor.test.js`, gedraaid door `./check.sh` (`node --test`, geen framework, geen npm). Laadt scriptblok 1, 2, 3 en 5 uit `cellarmentor.html` zelf in een browserstub en dekt alleen de pure regels (sync-beslissing, krimpbewaking, normalisatie, prijssleutel en credits gelijk aan de server, matchWine, foodCats, windowStatus, opslagquotum). Nieuwe pure regel: test erbij. Alles met scherm, sheets, camera of netwerk is **onverified by tests** en wordt met de hand in de browser getest; elk rapport zegt welk deel dat was. Een `node --test`-run na een stub-fout: "x is not a function" bij het laden.
- **Lint / typecheck:** `./check.sh` (node --check per scriptblok en alle blokken samen, typografische aanhalingstekens in tags, index.html gelijk aan de build, de tests). Edge Functions: `deno check supabase/functions/<naam>/index.ts` (deno staat niet op deze Mac; dan onverified; `npx -y esbuild@0.24.0 <bestand> --format=esm --outfile=/dev/null` vangt syntaxfouten).
- **Build:** `./build.sh` (maakt `index.html`; nooit met de hand bewerken).
- **Staging:** bestaat niet. Er is alleen lokaal en productie.
- **Productie, app:** `git push origin main`; Pages deployt in ± 30 s. **Brug:** `./brug.sh` (bouwt naar `../caveau`), daar commit en push; alleen nodig als sync- of back-upcode verandert. **Edge Functions:** `supabase functions deploy <naam> --project-ref dbzgrkipcoebglacsqwe`. **SQL:** met de hand in de Supabase SQL-editor, bestanden in `supabase/sql/`.
- **Omgevingen:** lokaal → productie. Supabase-project `dbzgrkipcoebglacsqwe` in `eu-west-1`.
- **Architectuurdocument:** `ARCHITECTURE.md`. **Besluitenlog:** `DECISIONS.md` (alleen toevoegen). **Jaargangtabel, bronnen en werklijst:** `JAARGANGEN.md`.
- **Publiek by design:** de Supabase publishable key in `cellarmentor.html`. Alle andere sleutels staan alleen als Supabase-secret (`CAVEAU_ANTHROPIC_KEY`, `STRIPE_*`, `RESEND_API_KEY`, `CRON_SECRET`), nooit in de repo. Uitzondering sinds 15 sep: `BRAVE_SEARCH_KEY` staat in de Supabase Vault (versleuteld in de database, te lezen via de SQL-functie `lees_geheim`, alleen service role; `supabase/sql/geheim-15sep.sql`), omdat hij is gezet vanuit een sessie zonder CLI. De Edge Function leest eerst de omgevingsvariabele, dus een echt secret met dezelfde naam wint zodra het er is.
- **Afwijkingen van de algemene regels, huidige stand (Max beslist of dit zo blijft):** commits gaan rechtstreeks naar `main` (één ontwikkelaar, geen PR-flow, geen branch-beveiliging); tests dekken alleen de pure regels; geen staging; foutmonitoring alleen voor mislukte AI-aanroepen (`ai_fouten`, kostenmail); SQL-wijzigingen zijn losse bestanden met de down-migratie als commentaarregel. `/ship` noemt deze punten in elk rapport.

## Architectuur
- **`cellarmentor.html` is de enige bron** (één bestand: CSS + markup + alle JS in genummerde script-blokken). `index.html` wordt gegenereerd en mag je nooit met de hand bewerken.
- Artifact-formaat: geen doctype/html/head/body-tags in cellarmentor.html; `head.html` + `build.sh` wikkelen hem naar `index.html`.
- Data: localStorage (`caveau_v1`, object `S`) + IndexedDB voor foto's. Cloud-sync via Supabase (document-LWW op `S.rev`, foto's incrementeel). `save(true)` = technische write zonder rev-bump/cloud-push.
- Supabase-project: `https://dbzgrkipcoebglacsqwe.supabase.co` (publishable key staat in de code, by design). Tabellen: `cellars`, `photos`, `profiles`, `ai_usage` (RLS per gebruiker), en alleen voor de server `wine_prices`, `wine_price_log`, `wine_paid`, `ai_fouten`. Auth: e-mail+wachtwoord; of "Confirm email" aanstaat is een dashboardinstelling (de code kent beide paden), controleer het daar.
- AI-routering (`callClaude`): eigen API-sleutel **alleen in de ontwikkelaarsstand** (`S.settings.devMode`, aan te zetten met zeven tikken op de titel in Instellingen; uitzetten wist de sleutel). `ownKey()` is de enige plek die hem teruggeeft; buiten die stand wordt een opgeslagen sleutel genegeerd, zodat niemand het tegoedmodel omzeilt. Met sleutel → rechtstreeks Anthropic; anders ingelogd → Edge Function **`ai`** (`supabase/functions/ai/index.ts`, secret `CAVEAU_ANTHROPIC_KEY`, telt credits in `ai_usage`; gratis 20 per maand (`FREE_CREDITS`), Plus 300 (`PLUS_CREDITS`), plan `unlimited` via dashboard → profiles; de soort aanroep moet in `KINDS` staan, anders 400). Sandbox (claude.ai-artifact) = AI uit, kelderregels (offline heuristiek) vangen alles op.

## Edge Functions
- Liggen in de CLI-indeling: `supabase/functions/<naam>/index.ts`, met `supabase/config.toml` voor de JWT-instelling per functie (`stripe-webhook` staat daar bewust op `verify_jwt = false`).
- Uitrollen: `supabase functions deploy <naam> --project-ref dbzgrkipcoebglacsqwe` (na `supabase login`).

## Wijzigingen uitrollen
1. Bewerk `cellarmentor.html` rechtstreeks.
2. Versienummer in `sw.js` ophogen (`cellarmentor-vN`). Strikt nodig alleen als iconen of manifest veranderen (de app zelf is netwerk-eerst met `cache:'no-cache'`), maar het is een handig versiestempel.
3. `./build.sh` (maakt index.html).
4. `./check.sh`: node --check per scriptblok én alle blokken samen (dubbele declaraties), **géén typografische aanhalingstekens** (“ ”) in tags (is eerder misgegaan), index.html gelijk aan de build, en de tests in `tests/`.
5. Functioneel testen in de browser (file:// werkt; localStorage beschikbaar).
6. Commit + push (Co-Authored-By-regel gebruiken); Pages deployt vanzelf (±30 s; poll met curl op een codefragment).
7. Optioneel: artifact herpubliceren vanaf `cellarmentor.html` (zelfde bestandspad = zelfde URL).

## Valkuilen
- **claude-sonnet-5/opus-5 denken standaard** (adaptive thinking) en dat telt mee in `max_tokens` → voor JSON-extractie `thinking:{type:"disabled"}` + ruim budget, anders afgekapte JSON ("Expected '}'").
- Git-identiteit van deze repo is bewust anoniem (`mpoons <…noreply…>`); niet globaal wijzigen.
- "Alles wissen" wist bij actieve sync eerst de cloud (`cloudWis`: foto's weg, leeg document met hogere rev) en pas dan lokaal; sessie, taal en weergave blijven staan. Sync heeft een krimp-guard (>80% minder wijnen → keuzesheet) en drie lokale reservekopieën (`caveau_backup_daily`/`_prev`/`_prev2`), die bij een vol localStorage-quotum als eerste sneuvelen (`schrijfState`, banner via `opslagVol`).
- Het smaakprofiel (`S.settings.smaak`) staat in de instellingen en synct dus niet mee; `cloudDoc()` neemt geen instellingen mee.
- API-sleutel en sessietokens nooit syncen/exporteren (zit zo in de code, zo houden).

## Status & vervolg (2 sep 2026)
- **Open na de verhuizing (15 sep 2026):** TransIP Doorstuurservice (± €7/jaar) bestellen voor mail aan @cellarmentor.com naar Gmail, daarna `CONTACT_MAIL` en `MAIL_FROM` omzetten en het domein bij Resend verifiëren (SPF/DKIM-records bij TransIP). Zodra alle testers over zijn: brug-repo `mpoons/caveau` mag blijven, kost niets. Handles YouTube/TikTok/X claimen; EU-woordmerk aanvragen (kl 9, 35, 42). Max wil aan de Doorstuurservice herinnerd worden.
- **Serverkant staat volledig live.** Fase 1 + 2 SQL gedraaid, alle drie de Edge Functions uitgerold en nagelopen: `ai` geeft 401, `billing` 401 "Niet ingelogd", `stripe-webhook` 400 "Ongeldige handtekening" (dus geen JWT ervoor en de handtekeningcontrole werkt). Secrets staan onder de juiste namen. Project draait in `eu-west-1` (Ierland), relevant voor de privacyverklaring.
- ⚠️ **De Anthropic-serversleutel verloopt 31 december 2026.** Loopt hij af, dan valt etiket- en wijnkaartlezen stil voor álle gebruikers. Half december vervangen en opnieuw zetten als `CAVEAU_ANTHROPIC_KEY`. Er staat een geplande herinnering voor 10 december.
- **Stripe draait sinds 15 sep 2026 op het live-account** (sk_live, webhook-endpoint naar `stripe-webhook` op events checkout.session.completed / customer.subscription.updated / customer.subscription.deleted, API-versie van het endpoint 2026-08-26.dahlia omdat het dashboard geen oudere aanbood; de code praat zelf met 2025-10-29.clover; de gebruikte velden bestaan in beide, maar de eerste echte betaling is de proef). Product `prod_VB8SYtxqKYfPps`, prijs `price_1UFvMiPvAFYixxSZA0dzD3vS`. Een afrekening is nu een echte afschrijving.
- **Plus is te koop sinds 15 sep 2026:** `PLUS_TE_KOOP = true` in cellarmentor.html (dit is de kill switch: op `false` verdwijnt de betaalknop). Stripe draaide tot 15 sep in testmodus, en een testafrekenpagina met een oranje TEST MODE-balk wil je geen eerste gebruiker laten zien. Tegoed geven doe je zolang met SQL (`bonus_credits`, of `plan = 'unlimited'`). Live gaan is die constante op `true`, plus in Stripe live-modus opnieuw product met belastingcode (`txcd_10103000`), prijs, webhook, klantportaal en sleutels.
- Stripe testmodus: account `acct_1UAm7oL0f2iIgWz0` (NL, onboarding niet af), product `prod_VB8SYtxqKYfPps`, prijs `price_1UFvMiPvAFYixxSZA0dzD3vS` (sinds 15 sep 2026, €2,99/mnd, btw inbegrepen) = `STRIPE_PRICE_PLUS`; de oude prijs was `price_1UAmBrL0f2iIgWz0Xc4PnL29`.
- **Supabase staat sinds 15 sep 2026 op het Pro-plan** ($25/mnd, factuur op naam van Ponsen Polutropon B.V., spend cap aan). Het plan hangt aan de organisatie "Ponsen Polutropon BV" (slug `idoemtmfrhyvhyevkifw`), niet aan het project: projecten pauzeren niet meer, er zijn dagelijkse back-ups (zeven dagen bewaard, Database → Backups), en de disk groeit vanzelf tot 8 GB. Iets anders hosten is een nieuw project in dezelfde organisatie; het inbegrepen compute-tegoed ($10) dekt één Micro-instantie, elk extra project kost ± $10/mnd extra. Het CellarMentor-project draait sinds 15 sep op Micro (t4g.micro, 1 GB, 2 cores; op Pro gratis, de overstap van Nano kostte één herstart van ± 2 minuten).

## Wat er sinds 1 sep is bijgekomen
- **Streamen.** `callClaude(content, maxTokens, kind, onText)` met `readSSE()`; de Edge Function laat de stroom ongewijzigd door en boekt het verbruik pas als er echt tekst gelezen is. Het scanformulier staat er meteen en vult zich onderweg (`scanPartial`/`vulFormLive`); velden die de gebruiker aanraakt komen in `scanTouched` en winnen van het antwoord (`behoudEigenInvoer`).
- **Scannen bij weinig licht.** De auto-vangst mat scherpte met een vaste drempel, waardoor de sluiter in een donker restaurant nooit klikte. Scherpte en beweging worden nu afgezet tegen de helderheid van het beeld zelf. Plus een zaklampknop waar de camera het aankan (Android; iOS Safari kan dit niet), `liftDark()` die donkere foto's opentrekt, en één compressieronde in plaats van twee (1400 px).
- **`matchWine` herschreven.** Naam en producent wegen apart: twee cuvées van hetzelfde domein zijn niet dezelfde wijn. Huiswoorden staan in `HUISWOORDEN`. Een andere jaargang is een andere fles.
- **Sync veiliger.** `S.syncedRev` houdt bij wat er als laatste geslaagd is gesynchroniseerd; de beslissing gaat over wie er iets veranderde, niet over welk revisienummer hoger is. Bij wijziging aan twee kanten volgt een keuzescherm (`askShrink` met stand `botsing`). `S.rev` is een teller in plaats van `Date.now()`. Uitloggen bewaart de uid, zodat een ánder account op hetzelfde apparaat niet de kelder van de vorige meeneemt. Twee reservekopieën, `krimpErgens()` weegt ook historie en verlanglijst, en mislukte fotouploads worden gemeld in plaats van weggeslikt.
- **Voorwaarden en privacy** onder Meer, met eigen adressen `#voorwaarden` en `#privacy` (hash-routering). Aanbieder is sinds 6 sep 2026 **Ponsen Polutropon B.V.** (KvK 77838335, de holding van Max): constanten `AANBIEDER`, `AANBIEDER_KVK`, `AANBIEDER_ADRES` (nog leeg; wettelijk verplicht zodra Plus te koop is, `aanbieder()` laat een leeg deel weg), `CONTACT_MAIL` en `LEGAL_DATUM`. De app-naam is nog niet gekozen; de BV is wel de rechtspersoon voor Stripe, Supabase, Anthropic en straks de app stores. Voor het afrekenen een scherm met prijs en een verplicht vinkje voor de directe levering.
- **Op je beginscherm.** `installBanner()` verschijnt meteen, ook bij een lege kelder, en verdwijnt na wegklikken of installeren. `beforeinstallprompt` geeft op Android één tik; op iOS staan de Safari-stappen uitgeschreven. **Sinds v79 (15 sep) per browser:** `iosBrowser()` leest de user agent (CriOS Chrome, FxiOS Firefox, EdgiOS Edge, zonder "Safari" een venster in een andere app) en `installUitlegIos()` noemt de plek van de deelknop (Chrome rechts in de adresbalk, Safari onderin) plus de stap Toon meer, want daar liep een tester in Chrome op vast. Sinds iOS 16.4 kunnen ook Chrome, Firefox en Edge op het beginscherm zetten; de oude zin "alleen Safari kan dit" is weg. Een venster in een andere app (Instagram, Facebook) krijgt alleen de wenk om in Safari of Chrome te openen. Ook: `fetchQuota` haalt `mail_herinnering` apart op (die kolom bestaat pas na `herinnering.sql`; zolang die niet gedraaid is gaf de gecombineerde query 400 en toonde Account "AI-tegoed: nog niet beschikbaar" voor iedereen).
- **Meerdere flessen tegelijk** (Meer, `viewImport`/`aiImport`, kind `import`): lijst plakken of tot 4 foto's van een lijst of staande flessen. Eén aanroep levert een reeks wijnen, de gebruiker vinkt aan wat mee mag. Drinkvensters komen van `estimateWindow`, niet van de AI. Dubbelen worden gemarkeerd en staan uit. Tik op een rij om bij te werken (hergebruikt `formFields`/`readForm`), en een veld boven de lijst zet de locatie op alle aangevinkte. Liggende flessen in een rek hebben geen zin, dat staat ook zo op het scherm.
- **Vensters en geschiedenis.** Een sheet sluit ook met een veeg naar beneden (alleen als `.sheet-in` bovenaan staat, anders is het scrollen). Elke sheet is een `pushState`-stap; de terugknop en de Safari-randveeg sluiten het venster (`popstate`; `sheetSkipPops`/`sheetNaPop` vangen de asynchrone `history.go` op). Een veeg vanaf de linkerrand (`wireSwipeBack`) sluit het bovenste venster, ook in de iPhone-app.
- **`readForm(root)` en `wireSeg(id, root)` kijken binnen één sheet.** Een sluitende sheet blijft 300 ms in de DOM, en met vaste veld-ids las `readForm` anders de velden van het verkeerde formulier.

## Beveiligings- en optimalisatieronde (2 sep 2026)
Twee reviews gedaan en verwerkt. Wat er nu geldt:
- **Alles van buiten wordt genormaliseerd** vóór het de kelder in gaat: `schoonAI()` op elk AI-antwoord (scan, herbereken, import), `schoonWijn/schoonHist/schoonWens` op back-ups (`doImport`) en het cloud-document (`adoptDoc`). Ids moeten aan `ID_OK` voldoen, jaartallen aan `jaarOk`, getallen aan `getalOk`, links aan `safeUrl` (alleen http/https). Formulier-attributen zijn geëscaped. Nieuwe velden die van buiten komen: hier toevoegen.
- **De server bouwt de prijsopdracht zelf** (`prijsPrompt` in de Edge Function) en negeert client-`messages` bij `kind:'prijs'`; een prijs komt alleen in `wine_prices` na controle (getal, confidence hoog/middel, URL op `PRIJS_SITES`), met `user_id` erbij. Het logboek `wine_price_log` heeft geen gebruikers-id meer en ruimt zichzelf na 30 dagen op.
- **Credits in één transactie**: SQL-functie `boek_credits` (advisory lock per gebruiker) boekt vóór de Anthropic-aanroep; mislukt die, of komt er bij streamen geen tekst, dan wordt de regel weer verwijderd. Kosten volgen ook de werkelijke beeldgrootte (`B64_PER_CREDIT`). Grenzen: body 8 MB, 30k tekens tekst, 8 beelden, 2 berichten, geen document-blokken. Foutdetails gaan naar `console.error`, de client krijgt een vaste tekst.
- **Webhook**: `plan_event_at` bewaakt de volgorde van Stripe-gebeurtenissen; checkout zet Plus alleen bij `payment_status === 'paid'`.
- **CSP** als meta in `head.html` (connect-src alleen Supabase en Anthropic). Nieuwe externe host: daar toevoegen, anders stil geblokkeerd.
- **Sync haalt eerst alleen het revisienummer** (`pullRev`); het document alleen bij een echte wijziging. `S.syncedTel` bewaart de tellingen van de laatste sync voor de krimp-guard. Elke fetch heeft een tijdslimiet (`tijdslimiet`). Gepullde foto's gaan niet meer terug omhoog.
- **Rendersnelheid**: `kelderPrijzen` en `smaakSets` zijn memo's die `save()` leegt. Achtergrondrenders lopen via `renderTenzijFormulier`. De stille herberekening bij een jaargangwijziging gebruikt de kelderregels (geen credit). Pairing- en waardeprompts sturen volgnummers in plaats van UUID's (`cellarIdx`); waardes gaan in blokken van 40.
- Reservekopieën bevatten geen caches meer; `dishCache` houdt drie richtingen per wijn.
- **Auth-links (2 sep).** Bevestigingsmail en herstelmail landen in de app via het hash-deel (`#access_token=…&type=signup|recovery`); het blok bij `appAdres()` zet de sessie, wist het adres en opent bij `recovery` het venster "Nieuw wachtwoord" (`openNieuwWachtwoord`, `PUT /auth/v1/user`). "Wachtwoord vergeten?" (`acVergeten`) roept `/auth/v1/recover` aan. Signup en recover sturen `redirect_to=appAdres()` mee; dat adres moet in Supabase als Site URL of redirect-URL staan: sinds 15 sep 2026 `https://cellarmentor.com/**` (Site URL `https://cellarmentor.com/`) én `https://mpoons.github.io/caveau/**` voor de brug. E-mailbevestiging aanzetten in het dashboard (Authentication → Providers → Email → Confirm email). Uitgavenplafond bij Anthropic staat aan (Max, 2 sep).
- **Huisstijl gekozen (2 sep):** richting E "Étiquette" als donkere stand (donkere grond, kaarten als ivoren etiketten met dubbele goudlijn, Playfair Display cursief, Karla) en richting F "Maison" als lichte stand (ivoor, lijnen in plaats van kaarten, woordmerk in Cormorant SC gespatieerd, Playfair cursief). Gebouwd (v50): de lichte tokens (`:root`, `[data-theme=light]`) zijn Maison, de donkere (`@media dark`, `[data-theme=dark]`) zijn Étiquette. In de donkere stand krijgen `.wcard .sheet .panel .match .dishcard .tile .menu` via een selector-scope hun eigen etiket-tokens (donkere inkt op ivoor, dubbele goudlijn via border + outline, bordeaux streep via `::before`); alles daarbuiten is licht op donker. Nieuwe component die als etiket moet ogen: in die selectorlijst (twee keer, media-query en data-theme) opnemen. Weergave kiezen kan in Instellingen (`S.settings.theme`: leeg volgt de telefoon, `light`, `dark`; `applyTheme()` zet `data-theme` op `<html>` en de theme-color-meta, meteen na het laden van `S`). Component-regels voor Maison staan onderaan de stylesheet ("Maison: de lichte stand"). Letters: Playfair Display (500/600, cursief voor namen) en Cormorant SC (woordmerk, eyebrows), zelf gehost in `fonts/` en in de service-worker-cache; de body blijft systeemletter. De mockups staan in het artifact "Huisstijlen voor de wijnkelder".
- **Ontwerpronde 3 sep (na het artikel van Lenny over AI-design):** tien richtingen plus een systeemronde, beoordeeld door een verse criticus-agent (fresh-context, alleen schermafbeeldingen). Uitkomst en keuze van Max: kelderlijst blijft (Maison), emoji's vervangen door lijn-iconen (`IK`/`ik(naam)`), flesscherm naar de catalogus-opzet (streepjesschaal `schaalHtml`, waarde groot met bron `waardeBlok`, voorraadregel `.drow`), Vanavond blijft met scores. Afgewezen: tijdlijn als beginscherm, rangorde "eerst, dan, ook goed". Alle mockups in het artifact "Huisstijlen voor de wijnkelder" (rondes een tot vijf).
- **Nog open uit de review**: captcha op signup (dashboard), `prijscache` heeft geen eigen limiet, foto's pullen gaat nog één voor één, en de zoekbalk rendert nog de hele kelderview per toets.

## Na de Caviste-vergelijking (3 sep 2026)
Caviste (Alexandre Rabottin, iOS/Android, juli 2026) naast CellarMentor gelegd; de naam Caviste is dus echt bezet. Overgenomen:
- **Afboeken met reden.** Het drink-venster heeft een keuzerij Gedronken / Cadeau / Verkocht / Kapot (`WEG`, `wireWeg`). Een andere reden dan gedronken zet `weg` op het historie-item, zonder sterren; `schoonHist` laat alleen bekende redenen door. Alleen echt gedronken telt in "Gedronken in [jaar]", "Onlangs gedronken" en het smaakprofiel (dat kijkt naar `rating`, en die is dan null). Historie toont 🎁 💶 💔.
- **CSV-import** (Meer → Meerdere tegelijk → CSV kiezen, `fCsv`): `csvParse` raadt het scheidingsteken uit de kopregel, `CSV_VELDEN` herkent kolommen op naam (CellarTracker, Vivino, Nederlands), `csvKoppel` toont een koppelscherm met een select per veld, `csvLees` bouwt `imp.found` en hergebruikt de controlelijst van de AI-import. Zelfde wijn (naam+producent+jaargang) wordt samengeteld, hoogstens 400 wijnen, jaargang 1001 (CellarTracker-NV) wordt null. Drinkvensters uit het bestand blijven staan (`drink_from/drink_to` gaan nu ook door `impEdit`/`impSave`/`impAdd`), een waarde uit het bestand krijgt `valueSrc:'eigen'`. Geen AI, geen credits.
- **Gerecht in de zoekbalk.** `filteredWines` roept `scoreWines` aan als geen enkele fles op de tekst past en een trefwoord uit `FOODS` aan het begin van een woord staat ("lam", niet "Lamothe"). Dan sorteert de lijst op affiniteit met een banner erboven (`UI.qGerecht`); de sorteerknop doet even niets. Een fles wint altijd van een gerecht.
- **Mail mij** (Instellingen → Herinnering per mail, alleen ingelogd). Vinkje → RPC `zet_mail_herinnering` → `profiles.mail_herinnering`; lokaal gespiegeld in `S.settings.mailMij`, bij het laden van het tegoed teruggelezen. Edge Function **`herinnering`** (`verify_jwt = false`, header `x-cron-secret`) loopt wekelijks via pg_cron + pg_net (SQL in `supabase/sql/herinnering.sql`), leest `cellars.data.wines`, rekent met dezelfde vensterregels als `windowStatus`, mailt via Resend alleen als er iets is (drink binnenkort, dit jaar op dronk gekomen, over de piek), hoogstens één keer per 6,5 dag (`profiles.mail_laatst`). **Nog te doen door Max:** SQL draaien (met de cron-secret ingevuld; `CRON_SECRET` staat sinds 3 sep, zie kostenmail), Resend-account (zonder eigen domein werkt `onboarding@resend.dev` alleen naar het eigen Resend-adres; voor gebruikers is een geverifieerd domein nodig, dus na de naam), secrets `RESEND_API_KEY`, `MAIL_FROM` zetten, `supabase functions deploy herinnering`. Privacyverklaring noemt Resend al.
- **Feedback en Over CellarMentor (3 sep).** Meer heeft een rij "Iets werkt niet, of een idee?" (`ACT.feedback`): mailto naar `CONTACT_MAIL` met flessenaantal, ingelogd/zonder account, beginscherm/browser en user-agent in de body. De tekst van Over CellarMentor noemt nu de sync.
- **Wekelijkse kostenmail** (Edge Function **`kosten`**, `verify_jwt = false`, header `x-cron-secret`; pg_cron `caveau-kosten` elke maandag 07:00 UTC, SQL in `supabase/sql/kosten.sql` met een placeholder voor het secret). Telt `ai_usage` van de afgelopen zeven dagen tegen de week ervoor (acties, credits, tokens, gebruikers, kosten per soort met Sonnet $2/$10 en Haiku $1/$5 per miljoen plus $0,03 zoekkosten per prijsopdracht, kosten per credit tegen de grens van $0,01), groei van `wine_prices`, missers in `wine_price_log`, aantal profielen en Plus. `CRON_SECRET` staat gezet (3 sep) en de cron loopt; zonder `RESEND_API_KEY`, `MAIL_FROM` en `KOSTEN_MAIL_TO` geeft de functie het overzicht als JSON terug in plaats van te mailen. Eerste meting (week tot 3 sep): 9 acties, 35 credits, $0,50, waarvan $0,49 de zoekagent; $0,014 per credit, boven de grens, maar dat is een week van alleen prijszoeken. Handmatig: `curl -X POST …/functions/v1/kosten -H "x-cron-secret: …"`.
- **Oranje als stil type.** `TYPES.oranje` heeft `stil:true`; `typesZichtbaar(extra)` laat hem pas zien als er een oranje wijn in kelder of verlanglijst staat, of als het formulier er een toont. `mapType` herkent oranje/orange/amber/skin contact (ging eerst naar wit); `estimateWindow`, `serveAdvice` en `foodW` (pairing-gewicht: leent van wit en rood) kennen hem. De AI-prompts noemen `oranje` in de typelijst.
- **Niet gebouwd, bewust:** een tweede foto (achteretiket) per scan. Dubbele beeldkosten en een extra vangststap voor winst die de appellation-kennis van het model meestal al levert. Kelderkaart (rijen × kolommen) loopt in een andere sessie.

## Testronde familie, verwerkt (3 sep 2026, v56)
Vragen van de familietesters (prijs €16 overal, Voliero 2017 op €22, Il Guercio 2024 als wit, voorraad bleef op drie, JSON-fout bij scannen) nagelopen en gebouwd:
- **Voorraad na +1 in het detail.** `save()` zet `renderNaSluiten` zodra er een venster open staat; `closeSheet` rendert dan bij het sluiten van het laatste venster (via `renderTenzijFormulier`, dus nooit over het scanformulier heen). Voorheen bleef de lijst achter het venster verouderd.
- **Herkansing bij een parse-fout.** `scanLezen()` (gedeeld door `handlePhoto` en `scanRetry`) leest gestreamd; is het antwoord geen JSON (`parseFout`: stroom halverwege afgebroken), dan meteen één keer opnieuw zonder streamen. Andere fouten (tegoed, netwerk) krijgen geen herkansing. De tweede poging kost een tweede credit; de eerste is al betaald zodra er tekst kwam. Foutmelding: `leesFout(e)`.
- **Type uit de druiven.** `typeControle(d)` na elke scan: `mapType(s, strikt)` geeft null bij onbekend; `kleurUitDruiven` (lijsten `RODE_DRUIVEN`/`WITTE_DRUIVEN`, langste treffer per druif wint) corrigeert wit↔rood bij eenstemmige druiven (`type_gecorrigeerd`) en vult een ontbrekend type aan. Blijft het onbekend: `type_onzeker`, geen knop actief in het formulier (`w.type === ''`), banner "Kleur niet van het etiket af te leiden", en `wineFromScanForm` weigert zonder type. `readForm` geeft nu `''` in plaats van rood als er niets gekozen is (`wineSave` valt terug op rood). De scanprompt vraagt de kleur van het etiket of uit druiven en appellation te halen, anders null.
- **Prijs.** (1) `value_confidence` (hoog/middel/laag) in scan- en herbereken-prompt; `waardeControle` gooit bij "laag" de schatting weg (`prijs_onzeker`), banner "Waarde onzeker", en `prijsNaScan` biedt dan opzoeken aan in plaats van een getal te tonen. (2) `prijsAlsBron(w)`: een aankoopprijs geldt als waarde (`valueSrc:'eigen'`, `valueVia:'aankoop'`, in het detail "aankoopprijs") zodra er geen waarde is of de schatting onder 80% van de aankoopprijs ligt; zelf ingevulde of opgezochte waardes blijven staan. Loopt bij `scanAdd` en `wineSave`. (3) Meting: `aiPrijs` stuurt `est` (de scan-schatting) mee, de Edge Function bewaart die als `wine_price_log.schatting` (SQL `supabase/sql/schatting-3sep.sql`, met terugval als de kolom ontbreekt), de kostenmail toont de mediaan schatting/gevonden en het aantal keer meer dan 20% te laag. **Brave Search als goedkope zoeklaag staat nog op de lijst** (zie geparkeerd hieronder), Max wil dat later oppakken.
- **Smaak uit proefnotities.** Geen woordenteller (tien keer "veel tannine" zegt niets zonder de sterren erbij). `notitiesLijst()` telt notities van minstens 8 tekens; per `NOTITIES_STAP` (10) nieuwe notities kan de gebruiker in Meer → Jouw smaak "Lees mijn notities" tikken (kind `smaak`, 1 credit): `smaakUitNotities()` laat het model in twee zinnen samenvatten waar deze drinker op let, met de sterren als weging; opgeslagen in `S.settings.smaak.notities {tekst, bijN, op}` en meegenomen in `smaakNote()`. `smaakWink()` geeft na de tiende nieuwe notitie één toast (4,8 s na de bestaande toast), nooit ongevraagd credits.
- **Elders geproefd.** Linkknop in het flesdetail (`ACT.proefElders`, `openProefElders`): sterren, plek, datum, notitie; historie-item met `proef:true, elders:true, wineId`, voorraad blijft staan. `proefLabel(h)` toont "geproefd, [plek]", "geproefd elders" of "geproefd bij aankoop"; `schoonHist` laat `elders` door.
- **Plek geven.** Wegleggen zonder locatie (scan én handmatig) geeft een toast met knop "Plek geven" → `openPlekGeven(id)`: alleen het locatieveld met chips en het rekraster (`rekKiesHtml`), `ACT.plekSave` via `rekNaBewaren`. De prijstoast na een scan wacht 5,6 s als die plek-toast er staat.
- **Weinig licht in de live-scanner.** Bij `mean < 70` zegt de hint dat het even kan duren en dat de knop ook werkt; de auto-vangst zelf is ongewijzigd. Live-scan pakt een frame van 1400 px direct van de camera (geen decode van een 12 MP-foto), dus in goed licht iets sneller dan uploaden; het AI-deel is gelijk.
- **Huishouden op één account** werkt (dat is de sync); grens is document-LWW met keuzescherm bij gelijktijdige wijzigingen. **Engelse versie**: gebouwd op 4 sep als vertaallaag, zie het kopje hieronder.

### Tweede ronde familievragen (3 sep 2026, v57)
- **Overzicht klikbaar.** `barRows(pairs, dot, act)` maakt de rijen knoppen; `filterType`/`filterLand`/`filterJaar` gaan via `naarLijst(f)` naar de kelderlijst met precies dat ene filter (`UI.fland`, `UI.fjaar`, bestaand `UI.ftype`/`UI.floc`). Actieve land- of jaargangfilter staat als chip met ✕ in de kelder (`chipLand`, `chipJaar`). Land "Onbekend" filtert op flessen zonder land, jaargang "NV" op flessen zonder jaar.
- **Nieuwe plek.** `openNieuweLoc(cb)` is één klein venster; `ACT.nieuwLocSave` maakt de locatie aan via `locEntry(naam, true)` en roept de callback. Gebruikt door de snelle wegleg-select na een scan (optie `::nieuw` in `locSelectOptions`; bij annuleren blijft de oude keuze staan) en door "+ Nieuwe plek toevoegen" op Meer → Locaties (`ACT.locNieuw`). Een lege locatie verschijnt in de lijst met 0 flessen. De 📍 en 🗑 daar zijn nu lijniconen `speld` en `prullenbak`.
- **Karafferen tegenover decanteren.** Sommelierjargon: karafferen is lucht geven (jonge, stevige wijn), decanteren is overschenken om bezinksel achter te laten (oude wijn). `serveAdvice` gebruikt nu het juiste woord per geval en legt karafferen in de tip zelf uit. Nooit vervangen door alleen "decanteren": dat is voor een jonge wijn gewoon onjuist.
- **Camerawenk op iOS (v58).** `camGeteld()` telt in `localStorage.caveau_cam` (per apparaat, bewust niet in het sync-document) hoe vaak de live-camera openging; bij `CAM_WENK_BIJ` (3, 4, 8, 10, 15) toont `camWenkToon()` na de vangst één toast met Instellingen → Safari → Camera → Toestaan. Alleen iOS (`isIOS`); Android onthoudt de toestemming zelf. Of het toestemmingsvenster echt verscheen is niet te zien, dus dit is een schatting.
- Schenkadvies zegt nu "Geef hem wat lucht in een karaf, een uur of twee" in plaats van het woord karafferen met uitleg; "Decanteren" blijft voor oude wijn met bezinksel.
- **Sessietoken (v60).** Oorzaak van "Serverfout 401" bij Precies opzoeken: het toegangstoken leeft een uur en werd alleen vernieuwd als het leeg was, nooit als het verlopen was. Nu `tokenVerlopen(t)` (leest `exp` uit het JWT, vernieuwt een minuut van tevoren), `sbToken()`, en `fnFetch(name, init)` voor alle Edge-Function-aanroepen (sbAI, sbFn, prijzenUitTabel) met één herkansing bij 401; `sbFetch` (auth en REST, dus ook de sync) doet hetzelfde. Nieuwe aanroepen naar de server: via `fnFetch`, niet los met `sbAccess`.
- **Op tafel: waar ligt hij.** De banner "Op tafel" toont locatie en vak vóór de temperatuur, het schenkvenster begint met "Ligt in …, vak B3", en in het detail heet het kopje dan "Plek in je kelder: Kast onder · B3" (`plekHtml`, alleen zolang de fles op tafel staat).
- **Pairing onthoudt en stuurt bij (v61).** `UI.pairDish` houdt het laatste gerecht in het veld; `pairRecent()` toont de acht laatste gerechten uit `S.pairCache` als kleine rijen "Eerder gevraagd" onderaan de pagina, onder het advies (v63; stond eerst als chips boven de invoer) (tik = `runPair(dish, false, stuur)`, dus het opgeslagen advies zonder credit). Onder een advies staat "Stuur het advies bij" (`pairStuur`/`pairStuurQuick`, alleen als AI beschikbaar is): `runPair(dish, true, stuur)` → `aiDish(dish, stuur)` zet de wens zwaar in de prompt; cache-sleutel is `norm(dish) + ' | ' + norm(stuur)`, de srcchip toont "· Liever wit". Zonder AI geen bijstuurpaneel, want de kelderregels kennen geen "liever wit".
- **Overzicht zonder stille afkapping (v62).** Per land stond op zes en per jaargang op acht rijen, nieuwste eerst, dus oudere wijnen verdwenen ongemerkt. Nu `ovStaven(key, pairs, dot, act)`: de eerste `OV_TOON` (8) rijen met "Alle N tonen ▾" (`UI.ovOpen[key]`, `ACT.ovAlles`). "Drink binnenkort" houdt vijf rijen maar krijgt "Alle N in de lijst →" (`ACT.soonAlle`, kelder gesorteerd op venster). Locatiechips in de formulieren en de bulkimport tonen alle plekken in plaats van de eerste acht. Regel: een lijst nooit stil afkappen; óf alles tonen, óf een zichtbare "alle N"-regel.
- **Chiprij kelder (v64, bijgesteld v65).** Locatie altijd vooraan, de rest op een vaste plek (array `rij` van `[aan, html]` in `viewKelder`). Actieve chips naar voren halen bleek storender dan scrollen; Max wil dat niet.
- **Waarde volgt de jaargang (v65).** `waardeIsSchatting(v)`: het veld is nog de AI-schatting (scan, `recalcValueRange`, of een bestaande wijn met `valueSrc:'ai'`) of leeg. Bij een jaargangwijziging (`change` op `f_vintage`) met zo'n schatting rekent `doRecalc(btn, false, {auto:true})` via de AI opnieuw mét de jaargang (1 credit, toast zegt dat); het venster gaat alleen mee zolang `vensterAuto` staat. Eigen of opgezochte waardes blijven staan; bij een opgezochte prijs van een andere jaargang komt een wenk om opnieuw te zoeken. Typte de gebruiker een jaargang terwijl de scan nog las, dan rekent `scanLezen` na afloop bij. De knop Herbereken overschrijft nu ook een bestaande schatting (deed hij niet). Lage zekerheid maakt het veld leeg.
- **Tegel "Gedronken in [jaar]"** opent een venster met de lijst van dit jaar (`ACT.tileGedronken`, rij → `histOpen`), zonder cadeau/verkocht/kapot; "Bekijk hele historie" sluit het venster eerst.
- **Leeftijd in de adviesprompts (v66).** `leeftijdNote()` (huidig jaar plus de opdracht leeftijd te wegen, met Primitivo jong/oud als voorbeeld) gaat mee in `aiDish`, `aiMenu` en `aiAsk`. Voorheen zag het model alleen de jaargang en de drinkstatus, zonder te weten welk jaar het is. De kelderregels (`scoreWines`) wegen leeftijd alleen via het drinkvenster.
- **Niet gebouwd:** één wijn op twee plekken (voorraad per plek). Het model heeft één `location` per wijn; de omweg is "Als nieuwe bewaren" bij de scanmatch, dan zijn het twee kaarten. Echte oplossing is `plekken:{locatie: aantal}` met een verhuisknop, en raakt kaarten, detail, rekvakken, afboeken en de sync. Max vindt dat voor nu een randgeval (een doos van zes gaat in zijn geheel naar één plek, of je scant ze los).

## Engelse versie (4 sep 2026, v67)
Gebouwd als **vertaallaag**, niet als refactor: de code en alle teksten in `cellarmentor.html` blijven Nederlands; het laatste scriptblok ("ENGELS: de vertaallaag") vertaalt wat op het scherm komt.
- `S.settings.taal` is `'nl'` of `'en'`; `taal()` en `LOCALE()` (`en-GB`/`nl-NL` voor datums) staan vroeg in blok 1. Bij de eerste opening zonder keuze opent `openTaalKeuze()` een venster (Nederlands / English); daarna te wijzigen in Instellingen → Taal / Language (`ACT.setTaal`). Bestaande gebruikers zien het venster één keer.
- **Woordenboek `EN`** (exacte, getrimde tekst → Engels) en **`EN_RE`** (regexregels voor teksten met een getal of naam erin, vervanging mag een functie zijn). `tr(s)` vertaalt één tekst; wat niet gevonden wordt blijft Nederlands staan, dus er breekt nooit iets. Een **MutationObserver** op `document.body` vertaalt elke toegevoegde knoop en elke tekstwijziging (view, sheets, toasts, camerahints); `vertaalNode` slaat `SCRIPT`, `STYLE` en `TEXTAREA` over (gebruikersnotities blijven onaangeroerd) en vertaalt ook `placeholder`, `aria-label`, `title`, `alt`. Het origineel wordt in `__nl` bewaard zodat `herstelNL()` bij terugschakelen de vaste navigatie terugzet. `applyTaal()` zet `<html lang>` en `document.title`.
- **Nieuwe zichtbare tekst = nieuwe regel in `EN`** (of een regexregel als er een getal in zit). Dekking meten: de inventaris-scripts uit deze sessie trokken 619 letterlijke teksten uit de scriptblokken; alles behalve de juridische pagina's staat erin.
- **AI in het Engels**: `taalNote()` hangt aan `styleNote()` (in elke prompt) en zegt in het Engels te schrijven ook waar de prompt "Nederlands" vraagt; JSON-sleutels blijven. Gebruikersinvoer (gerecht "Biefstuk" via chips, `data-d`) blijft Nederlands in de prompt, het antwoord komt in het Engels.
- **Bewust Nederlands gebleven**: Voorwaarden en Privacy (juridisch bindend; in de Engelse stand staat er een banner boven via `legalNote()`), de `FOODS`-trefwoorden voor gerechtherkenning in de zoekbalk (Engelse zoektermen als "lamb" starten dus geen gerechtmodus), en wat een gebruiker zelf typt.
- Getest in de browser: navigatie, kelder, detail, formulier (textarea met "Rood" bleef "Rood"), overzicht, pairing, account, instellingen, dynamische toast, eerste-keuzevenster na herladen, en terug naar Nederlands.

## Prijzen met bron (6 sep 2026, v68)
Uitgangspunt na de familietest (€16 op alles): **geen getal zonder bron**. Drie lagen gebouwd, de vierde (Wine-Searcher API, $250/mnd) is voor als er inkomsten zijn.
- **Bron of niets.** `metBron(w)` = waarde aanwezig en `valueSrc !== 'ai'`; `bronWaarde(w)` = die waarde, anders de aankoopprijs, anders 0. `indicatie(w)` is de AI-schatting (`valueSrc:'ai'`) met band. Kelderwaarde (`cellarValue`, tegel met "N flessen zonder prijs" via `zonderPrijs()`), `kelderPrijzen`/`plekInKelder` en de topfles-badges rekenen alleen met `bronWaarde`. Het detail (`waardeBlok`) toont zonder bron "Nog geen prijs met bron. Indicatie van de sommelier: €a tot €b, geen echte prijs." en het grote getal alleen bij bron. Het waardeveld in formulieren toont een indicatie als placeholder ("indicatie 22"), nooit als invulling; opslaan zonder typen bewaart de indicatie als `valueSrc:'ai'` (scanAdd, wineSave), typen maakt hem `eigen`. `doRecalc` zet een nieuwe schatting in de placeholder. Een waarde zonder bronveld (van vóór 2 sep) geldt als `eigen`; `schoonWijn` normaliseert `valueSrc` naar ai/zoek/eigen/samen.
- **Zoeklaag (kind `prijs`, 1 credit).** Server: `braveZoek(wijn)` doet één Brave-zoekopdracht (`BRAVE_SEARCH_KEY`, `country=NL`, tien treffers met extra snippets), `leesPrompt` laat Haiku de prijs uit de fragmenten lezen; de bron-URL komt uit het genummerde treffer (`result`), nooit uit het model. Geen treffers: credit terug, log-regel model `brave`, antwoord `value:null`. Plafond `BRAVE_DAG_MAX` (400 per dag over alle gebruikers, geteld in `wine_price_log` op model `%brave%`). Zonder sleutel valt `prijs` terug op de zware agent. Kosten ± $0,005 Brave + ± $0,003 Haiku per wijn; met de gedeelde tabel één keer per wijn. **Zware agent is nu kind `prijsdiep` (5 credits)**, alleen na een miss van de zoeklaag: toast "Dieper zoeken" na een scan, en de detailknop wordt "Niets gevonden. Dieper zoeken · 5 credits". Client: `aiPrijs(w, diep)`, `prijsAchteraf(w, meld, diep)`, `creditCost('prijs')` 1 / `'prijsdiep'` 5. `prijsNaScan` zoekt nu **standaard meteen** na elke scan zonder bron (`S.settings.prijsAuto`, uit te zetten in Instellingen → Prijzen); `prijsBulk` pakt alle flessen zonder bron (1 credit per stuk).
- **Gemeenschapsprijzen (kind `betaald`, geen credit).** Opt-in `S.settings.deelPrijzen` (Instellingen → Prijzen, alleen ingelogd); aanzetten deelt meteen alle bestaande aankoopprijzen (`deelAlles`), daarna elke nieuwe of gewijzigde aankoopprijs (`deelBetaald` in scanAdd en wineSave). Tabel `wine_paid` (SQL `supabase/sql/betaald-6sep.sql`, één regel per gebruiker per wijn, RLS zonder policies: alleen de service role). `prijscache` geeft `paid[]` terug: uitschieters buiten 0,4× tot 2,5× de mediaan vallen weg, daarna alleen bij ≥2 overgebleven gebruikers (n, low, high, med). De client toont het vanaf 2 en neemt het pas vanaf 3 als waarde over (`valueSrc:'samen'`); één regel per gebruiker per wijn, bedrag 1 tot 100.000. Client zet `w.samen` en, zonder andere bron, `valueSrc:'samen'` met `valueBron {name:'CellarMentor-gebruikers', n}`; detail toont "wat 3 CellarMentor-gebruikers betaalden, €14 tot €21". Privacyverklaring noemt Brave en het delen.
- **Nog te doen door Max:** SQL `betaald-6sep.sql` draaien; Brave-sleutel aanmaken (api-dashboard.search.brave.com, creditcard, $5 tegoed per maand) en zetten als `BRAVE_SEARCH_KEY` (`supabase secrets set BRAVE_SEARCH_KEY=…`). Tot die tijd kost elke automatische zoekopdracht na een scan de zware agent (± $0,06) voor 1 credit; zet desnoods `prijsAuto` uit tot de sleutel staat.
- Bewust niet gebouwd: bon of prijskaartje scannen (mensen vullen de aankoopprijs zelf in).
- **Volgende laag, afgesproken 6 sep: affiliate-productfeeds.** Nederlandse en Belgische wijnwinkels (Gall, Wijnvoordeel, Grapedistrict, Wijnbeurs, e.a.) publiceren via Awin, TradeTracker en Daisycon dagelijkse productfeeds (naam, jaargang, prijs, voorraad, deeplink). Plan: nachtelijke job (pg_cron + Edge Function `feeds`) laadt ze in een tabel `shop_prices` (key volgens `prijsSleutel`, shop, prijs, url, gezien_op); `prijscache` geeft die vóór alles terug als "winkelprijs, Gall.nl, vandaag"; in het flesdetail een knop **"Bestel opnieuw"** met de affiliate-deeplink (commissie 3 tot 8 procent, dat is de eerste inkomstenbron naast Plus). Voorwaarde: aanmelding bij de netwerken vraagt een eigen website met naam en domein, dus dit wacht op de hernoeming. Privacyverklaring: noemen dat een klik op zo'n link naar de winkel gaat met een partnercode, zonder gegevens over de gebruiker.

## Pairing: trefwoorden en voorselectie (6 sep 2026, v70)
Aanleiding: "Zeewolf met kalfsfond-witte-wijn-saus en knolselderijpuree" kreeg gerijpt rood, omdat `FOODS` op substrings zocht en "ree" in "puree" zat. Nu:
- **`foodCats(text)`** is de enige plek die gerechten herkent (kelderregels, zoekbalk, kaartgids). Trefwoorden gelden per heel woord; `kip*` laat het woord doorlopen (kippenbout), `*risotto` laat er iets voor staan (champignonrisotto). Regexen staan in `kwReCache`. Nieuwe categorie `kalf` (wit 3, rood 3); lijst uitgebreid met vissen, sauzen (beurre blanc, witte wijnsaus) en Engelse basiswoorden. `gegrild`/`gerookt` zijn bewust weg bij BBQ (gerookte zalm kreeg rood).
- **`cellarLines`**: tot `CELLAR_ALL` (100) flessen gaat de hele kelder mee naar de sommelier (± 35 tokens per fles, dus een cent op honderd flessen). Daarboven een ruime voorselectie: 70 op score, plus de vier meest urgente per kleur, plus wat op moet. Eén verkeerd gelezen gerecht kan de AI dus nooit meer een hele kleur ontnemen.
- **Kelderregels-redenen per fles (v71).** `regelReden(x)` zegt wat déze fles onderscheidt (druif, herkomst, kleur: "Chardonnay uit Bourgogne: een klassieke druif bij schaal- & schelpdieren"); de regel zelf staat één keer boven de lijst ("Kelderregel: …"). Voorheen stond vijf keer dezelfde zin onder elke kaart. `scoreWines` geeft per wijn `cat`/`catW` (de best passende categorie) terug. Engels via regexregels in `EN_RE` plus `trLaag()` voor woorden die midden in een zin staan.
- **Gerechtveld heeft een kruisje** (`.swis`, `ACT.pairWis`, `pairWisToon()` via een `input`-listener); het advies eronder blijft staan tot er een nieuw gerecht is. Toast: tekst in een `<span>`, `width:max-content` en `flex-wrap`, zodat een lange melding met knop ("Zoek diep · 5 credits") niet meer in een smalle kolom breekt; de knop gaat dan rechts onder de tekst.
- **Zoete appellations (v72).** `ZOETE_APPELLATIES` + `zoetVolgensNaam(w)` (naam, appellation, streek; niet bij "sec"/"brut"/"dry"); `pairType(w)` geeft `zoet` voor een fles die als wit in de kelder staat maar een Quarts de Chaume, Sauternes, Tokaji Aszú enz. is. Gebruikt door `scoreWines`, `regelReden`, `dishesForWine` en de prompt-regel (`wineLine` zet "(zoete appellation, dessertwijn)" achter het type). `typeControle` corrigeert een scan van wit naar zoet op dezelfde lijst. Daarnaast: een fles zonder enige spijs-fit (`fit <= 0`) valt nu uit de kelderregels; voorheen tilde de urgentiebonus (+1,4 bij "drink binnenkort") een dessertwijn alsnog in de lijst bij een bisque.
- **Advies zonder AI wordt altijd benoemd (v73).** `zonderAiHtml(reden)` is één banner voor Mijn gerecht (`runPair`), Mijn wijn (`renderDishes`) en Wijnkaart (`runMenu`): "Advies zonder de sommelier-AI. [reden]. Dit komt uit de standaardregels … Daardoor zijn dit misschien niet de allerbeste keuzes." De reden komt uit `aiRedenUit()` (niet ingelogd, sandbox) of `leesFout(e)` (tegoed op, sessie, netwerk). De bronchip heet dan "Standaardregels · zonder AI" (`bronLabel`; intern blijft de waarde `'Kelderregels'`). Een stil ingeslikte fout zag eruit als een advies met volle overtuiging.
- **Syntax-check ook als één geheel.** `node --check` per scriptblok ziet geen dubbele `const` over blokken heen (`hoofd` bestond al in blok 2). Plak alle blokken achter elkaar in één bestand en check dat ook.

## Fresh review verwerkt (7 sep 2026, v74)
Rapport: `~/Downloads/fresh-review-caveau-2026-09-07.md`. Vijf stappen gebouwd, elk een commit:
1. `ACT.wipe` wist met sync aan eerst de cloud via `cloudWis()`; vier toasts uit de sleuteltijd gebruiken `aiRedenUit()`; README herschreven.
2. Server: `KINDS` (vaste lijst, anders 400), `okUrl` ook op het Brave-pad, tabel `ai_fouten` (`supabase/sql/fouten-7sep.sql`) gevuld door `logFout` in `ai`, kostenmail escapet en toont fouten per status; tarieven `prijs` $0,005 en `prijsdiep` $0,03 extra. `ai` en `kosten` uitgerold op 7 sep.
3. `schrijfState()`: bij een vol quotum eerst de reservekopieën weg, dan `opslagVol` met banner en back-upknop in de kelder.
4. `syncBesluit(localRev, remoteRev, syncedRev, kopBestaat)` als pure functie; `syncNow` voert alleen uit. Bug eruit: in de stand 'push' met bekende telling was `remote` een placeholder (`_kop`), en "Cloud overnemen" adopteerde lege rijen; nu eerst `pullDoc()`. `tests/cellarmentor.test.js` (22 tests) via `check.sh`.
5. Documentatie gelijkgetrokken (dit bestand, ARCHITECTURE.md, DECISIONS.md, privacy en voorwaarden: herinneringsmail noemt producent, aantal en plek; credits voor prijs, dieper zoeken en indicaties), `supabase/.temp` uit git.
- **Nog te doen door Max:** `supabase/sql/fouten-7sep.sql` draaien in de SQL-editor (tot die tijd zegt de kostenmail dat het logboek ontbreekt; niets breekt).
- **Uit de review nog open** (klein, elk een eigen taak): `unlimited` zonder daglimiet; `refresh:true` laat elke gebruiker een gedeelde prijs overschrijven; `adoptDoc` normaliseert `pairCache`/`recipeCache`/`tonight`/`cellarName` niet; `schoonWijn` laat onbekende velden door (`{...w}`); `EN_RE` vertaalt ook getoonde notities; modellen zonder datum in de id; `supabase-js@2` zonder minor-pin; `sbRefresh` logt stil uit bij 400/401/403; `ACT.wishDel` zonder bevestiging; de service worker faalt in zijn geheel als één font ontbreekt; `billing` maakt bij een mislukte profielupdate een tweede Stripe-klant.

## Prijzen actueel en beter gevonden (15 sep 2026, v79)
Aanleiding: "de prijzen zijn niet altijd goed up to date". Eerst gemeten in productie (15 sep): de prijstabel heeft 8 rijen, alle uit september, en `wine_price_log` heeft 27 zoekopdrachten, **allemaal via de zware agent (`claude-haiku-4-5`, gemiddeld 42k invoertokens), geen enkele via Brave**. De Brave-laag van 6 sep is dus nooit live gegaan omdat `BRAVE_SEARCH_KEY` niet is gezet. Van de 27 vond de agent er 9 een prijs (een derde), en 7 van die 9 zijn Amerikaanse Wine-Searcher-lijsten in dollars, omgerekend met een vaste koers plus btw-gok. Het probleem is dus dekking en herkomst, niet veroudering. Gebouwd:
- **Server, zware agent:** `PRIJS_SITES` uitgebreid met ah.nl, jumbo.com, grapedistrict.nl, henribloem.nl, okhuysen.nl, colruyt.be, delhaize.be, hawesko.de en vicampo.de (de winkels waar de meeste flessen vandaan komen), en de API-zoekfunctie krijgt `user_location` Amsterdam/NL (`ZOEK_PLEK`), zodat de resultaten euro's uit Nederland en België zijn in plaats van dollars uit de VS.
- **Server, zoeklaag (Brave):** levert een zoekopdracht mét jaargang niets op, of leest Haiku er geen prijs uit, dan volgt één zoekopdracht zónder jaargang ("… wijn kopen") en een tweede leesbeurt (`braveZoek(w, true)`, logboekmodel `+brave2`). Winkels noemen vaak alleen de jaargang die in het schap ligt; rule 2 van `leesPrompt` maakt daar dan "middel" van. Tot twee Brave-zoekopdrachten per prijsvraag, dagplafond `BRAVE_DAG_MAX` telt nog per prijsvraag.
- **Server, houdbaarheid:** `prijsVerouderd(row)` (ouder dan `PRIJS_VEROUDERD_DAGEN` = 90). "Prijs opzoeken" op een verouderde rij geeft niet meer de tabel terug maar zoekt opnieuw (1 credit) en vervangt de rij. `prijscache` geeft verouderde rijen nog wel terug (met datum) en ververst ze daarna **op de achtergrond** via de zoeklaag (`versPrijzen`, `EdgeRuntime.waitUntil`): hoogstens `VERS_PER_AANROEP` (3) per aanroep en `VERS_DAG_MAX` (40) per dag, alleen met Brave-sleutel, zonder credit, logboekmodel `+brave+vers`. Een automatische verversing vervangt de prijs alleen als hij tussen 0,4× en 2,5× de oude ligt (er kijkt geen mens mee); mislukt het, dan blijft de rij twee weken met rust. Kostenmail toont het aantal verversingen apart.
- **Client:** `tabelPrijsPast(w, p)` beslist of een tabelprijs een fles mag overschrijven: nooit een eigen waarde of aankoopprijs (dat gebeurde wél, tegen de regel van v65 in), wel zonder bron, wel over 'samen', en over een eerder opgezochte prijs alleen als de tabel een nieuwer datapunt heeft. `prijsTabelRonde` neemt nu alle flessen behalve 'eigen' mee, dus een verversing door een ander of door de server komt bij iedereen aan bij de eerste opening van de dag. `valueAt` is de dag van het datapunt (`datumOf(r.at)`), niet de dag van ophalen; voor 'samen' de datum van het laatste gedeelde bedrag. Tests: `tabelPrijsPast`, `datumOf`, en `prijsVerouderd` van de server (28 tests).
- **Live gezet op 15 sep 2026 (avond), alles vanuit de Claude-sessie:** Brave-sleutel in de Vault (`lees_geheim`, zie §0), `ai` (versie 40) en `kosten` (versie 15) uitgerold via de Supabase-MCP, `schatting-3sep.sql` en `fouten-7sep.sql` gedraaid (kolom en tabel bestaan nu), `main` gepusht. De sleutel is gezet zonder proefzoekopdracht (api.search.brave.com is vanuit de sessie geblokkeerd); de eerste "Prijs opzoeken" van een gebruiker bewijst hem: in `wine_price_log` hoort dan model `claude-haiku-4-5+brave` te staan. Staat er `claude-haiku-4-5` zonder toevoeging, dan leest `braveSleutel()` niets en hoort er een regel `lees_geheim` in de functielogs.
- **Wil je de sleutel ooit als echt secret:** `supabase secrets set BRAVE_SEARCH_KEY=…` en daarna `delete from vault.secrets where name = 'BRAVE_SEARCH_KEY'`; de code hoeft niet mee.

### Tweede ronde (16 sep 2026, v81): Sonnet leest, agent zoekt vrij
Eerste acht zoekopdrachten via Brave (avond 15 sep): 5 gevonden, ± 3.300 tokens per fles, maar Haiku koos een Lidl-folder (€3,49) en een Duitse retourwinkel terwijl Nederlandse winkels in dezelfde lijst stonden, en de link bleef weg voor winkels buiten `PRIJS_SITES`. Max: "in claude.ai vindt hij hem bijna altijd". Gebouwd:
- **`MODEL_LEES` = Sonnet 5** voor de leesbeurt op Brave-fragmenten (± een halve cent extra per fles) en voor de achtergrondverversing. `MODEL_BY_KIND.prijsdiep` = Sonnet 5 met `web_search_20260209`, vijf zoekrondes, **geen domeinlijst** en `user_location` NL, dus zoals claude.ai zelf; `max_tokens` 4000. De Haiku-terugval van `prijs` zonder Brave-sleutel houdt de sitelijst (dure agent voor één credit). Kostenmail rekent `prijs` en `prijsdiep` nu op Sonnet-tarief.
- **`rangschik(treffers, VOORKEUR, RUIS)`**: bekende winkels (sitelijst plus gevoslijterij.nl, perfectewijn.nl, whiskyvanzuylen.nl e.a.) rang 0, onbekend 1, folders/outlet/retour/veiling 2; binnen een rang de volgorde van Brave. `leesPrompt` zegt dat een lager nummer betrouwbaarder is en dat een europrijs vóór een omgerekende gaat. Test erbij.
- **Link op het Brave-pad altijd bewaard** (`braveUrl`): het adres komt uit de zoekmachine, niet uit het model; `okUrl` met de vaste lijst geldt alleen nog voor het agent-pad, waar het model het adres zelf opschrijft.
- **"Opnieuw opzoeken" altijd zichtbaar** in het flesdetail (`plekHtml`), ook bij een verse opgezochte prijs; de server beschermt de gedeelde tabel (`prijsMagVervangen`). Voorheen pas na een jaar.
- Uitgerold: `ai` en `kosten` opnieuw via de Supabase-MCP, `main` gepusht, brug gebouwd.

### Derde ronde (16 sep 2026, v82): identiteitslaag, trap en herkansing
Casus: een Provence-rosé gescand als producent "Famille de Wulf Domaine des Annibals", naam "Cuvée des Annibals", 2025. Twee agents vonden gisteravond prijzen voor "Cuvée des Annibals Rosé van Château des Annibals" en gooiden die weg omdat de familienaam en de jaargang niet klopten; de leesregel ("twijfel je, laat weg") keurde het goede antwoord af. Vanochtend liep de Sonnet-agent tegen storingen van de API-zoekfunctie aan, elke storing telde als ronde, 75.000 tokens voor niets. Grote apps zoeken niet per fles op het web maar lossen eerst de identiteit op (Vivino via etiketdatabase, Wine-Searcher via crawler, supermarkt-apps via EAN). Gebouwd:
- **Zoekidentiteit uit de scanner.** De scanprompt vraagt `zoek_producent` en `zoek_naam` (de kortste vorm waaronder een winkel de fles verkoopt: geen eigenaarsfamilie, rechtsvorm of importeur; alleen de cuvéenaam zonder Cuvée/AOP). Op de wijn als `zoekProducer`/`zoekNaam` (`schoonAI`, `schoonWijn`, `scanAdd`, `scanMerge`); `wijnId(w)` is wat er naar de server gaat. **`prijsSleutel` gebruikt de zoekidentiteit** als die er is (client en server gelijk, test), zodat twee scans van dezelfde fles dezelfde tabelrij delen; oude flessen houden hun oude sleutel.
- **Server:** `kortVorm()` als vangnet voor flessen zonder zoekidentiteit (eigenaarsfamilie vóór een domein en rechtsvormen weg), `zoekId(w)`, `KLEUR`, `zoekTreden(w)`: trap van hoogstens drie Brave-zoekopdrachten (precies met jaargang; zonder jaargang; producent plus appellation of streek plus kleur, zonder cuvéenaam), dubbelen weg. `zoekViaBrave(w, key)` daalt de trap af tot er een prijs is en wordt gedeeld door de zoeklaag en de achtergrondverversing (die riep sinds vanochtend Sonnet aan zónder `thinking` uit; nu wel). `leesPrompt(w, treffers, trede)`: regel 1 zegt nu wat GEEN andere wijn maakt (familienaam, Château/Domaine, Cuvée/AOP/Rosé, importeur, jaargang) en per trede wat nog telt. `prijsPrompt` (agent) met dezelfde trap en regel. Logboekmodel `+brave`, `+brave2`, `+brave3` per trede.
- **Agent met herkansing:** `pause_turn` wordt voortgezet (hoogstens twee keer), `telZoekrondes()` telt geslaagde en mislukte zoekrondes, en zonder prijs met storingen en hooguit één geslaagde ronde volgt na drie seconden één verse poging (`+herkansing` in het logboek).
- Tests: 30 (zoekidentiteit, trap, sleutel op zoekidentiteit). Volgende stappen, afgesproken: streepjescode (EAN) als tweede identiteit, daarna de affiliate-feeds.

### Vierde ronde (16 sep 2026, v83): streepjescode als tweede identiteit
Uitgangspunt van Max: nooit ten koste van de scanflow. Daarom drie plekken, geen van alle in de scan zelf:
- **Gratis meelezen.** De scanprompt vraagt `ean` (de cijfers onder een streepjescode als die toevallig op de foto staat). `schoonAI`/`schoonWijn` houden alleen een geldige EAN-13 of EAN-8 (`eanGeldig`, controlecijfer), op de wijn als `w.ean`, mee in `wijnId()`.
- **Later, los.** In het flesdetail staat "Streepjescode scannen" (`eanKnop`, alleen zonder prijs met bron, zonder ean, met camera, niet in de sandbox). Dat opent de live camera in de stand `barcode` (`openLiveScan('barcode')`, geen galerijknop, geen auto-vangst van het etiket): elke 260 ms `barcodeTick`, eerst `BarcodeDetector` (Android/Chrome), anders de **eigen lezer** zonder bibliotheek: `eanUitBeeld` neemt negen beeldregels rond het midden, `eanRunsUitRij` maakt er strepen van (drempel midden tussen licht en donker), `eanUitRuns`/`eanLees` leggen de strepen per cijfer tegen `EAN_L`/`EAN_G` (pariteit geeft het eerste cijfer), beide richtingen, en alleen een code met kloppend controlecijfer telt. De sluiterknop probeert één keer op volle resolutie. Gevonden: `eanGevonden` bewaart `w.ean`, trilt, en start meteen `prijsAchteraf` (1 credit). Tests: `eanGeldig`, `eanUitRuns` op synthetische strepen (drie EAN-13's, een EAN-8, op zijn kop, met storing, en een kapotte streep geeft null).
- **Server:** `eanVan(w)`, `zoekTreden` geeft nu treden met soort (`ean` vooraan, dan `precies`, `zonderJaar`, `zonderCuvee`); levert dat niets op en is er een EAN, dan `offNaam` (Open Food Facts, vrij, alleen het nummer, 4 s) als extra trede `off`. `leesPrompt` per soort: een resultaat met het nummer is precies deze fles. Prijstabel: kolom `ean` (`supabase/sql/ean-16sep.sql`, gedraaid 16 sep), `prijs` kijkt na de sleutel ook op ean, `prijscache` geeft een rij op ean terug onder de sleutel van deze fles, upsert bewaart de ean. Logboek: `+brave+ean`, `+brave+off`. Privacyverklaring noemt Open Food Facts.
- **Eerste echte test op Max' iPhone (16 sep, 11:35) mislukte**, en de foto liet zien waarom: op een wijnfles staat de code gedraaid (strepen horizontaal) en de lezer las alleen horizontale regels; bovendien werkte hij op een verkleind beeld (streepje van anderhalve pixel). **v84:** `eanUitBeeld` leest dertien regels én dertien kolommen op volle resolutie (één `getImageData` van het hele beeld), `eanRunsUitRij` gebruikt een plaatselijke drempel (gemiddelde over een venster van een twaalfde van de lijn, tegen glans en schaduw op een fles; test met lichtverloop). De sluiterknop probeert eerst zelf en stuurt anders de foto naar de sommelier (`eanViaModel`, kind `ean`, Haiku, 1 credit, alleen JSON `{"ean":…}`); de hint na vijf seconden zegt dat. Server: `KINDS` en `MODEL_BY_KIND` kennen `ean`; kostenmail rekent Haiku-tarief. Nog niet opnieuw op een echte fles getest. Tests: 33.
- **v85, open detail ververst zichzelf.** Max zag na een scan "Nog geen prijs met bron" blijven staan terwijl de prijs allang binnen was: `renderTenzijFormulier` tekent niets zolang een venster open staat (terecht, tegen het wissen van een formulier), en de lijst erachter kwam pas bij het sluiten (`renderNaSluiten`). Nu merkt `openDetail` het prijs- en plekvak (`data-prijsvak`, `data-plekvak`) en herschrijft `versPrijsvakken` alleen die twee ter plekke, zonder het venster te sluiten en zonder stap in de geschiedenis; `renderTenzijFormulier` roept dat aan als er vensters open staan. Zolang `prijsAchteraf` loopt staat de fles in `prijsBezig`: het waardevak toont "De sommelier zoekt de prijs…" en de knop "Prijs opzoeken" is dan een bezig-regel (anders twee credits voor dezelfde fles); een tweede `prijsAchteraf` voor dezelfde fles wordt genegeerd. Tests: 34.
- **Volgende laag, niet gebouwd:** de affiliate-productfeeds (Awin, TradeTracker, Daisycon; zie "Prijzen met bron"). Sinds de naam en het domein er zijn is de aanmelding mogelijk. Dat is de echte "grote database": dagelijkse winkelprijzen met deeplink, en de eerste inkomsten naast Plus. Wine-Searcher API ($250/mnd) blijft wachten op inkomsten.

## Drinkvensters en jaargangen (16 sep 2026, v86)
De reden dat dit bestaat: een wijnverzamelaar kreeg te horen dat zijn vintage champagnes over de piek waren terwijl 2008 en 2012 nog decennia voor zich hebben. Twee oorzaken, beide structureel. De app kende geen jaargangen (elke vintage champagne vijf tot vijftien jaar, ongeacht het jaar) en "Over de piek?" was een klif een jaar na `drinkTo`.
- **Jaargangtabel.** `STREKEN` in scriptblok 3: achtendertig streken met trefwoorden en een string `j` van `jaar:niveau`, 1 (moeilijk) tot 5 (uitzonderlijk). `JAARTABEL` pakt die één keer uit. `streekVan(w)` kiest de eerste passende streek (specifiek vóór algemeen: Sauternes vóór Bordeaux), met een cache op type plus herkomsttekst; een streek met `t:[...]` geldt alleen voor die types, en zo staan de Bourgogne rood en wit apart. `jaargangOordeel(w)` geeft `{streek, jaar, niveau, woord}`, of null zonder jaargang of zonder streek; een jaar buiten de tabel geeft niveau 3 en `woord: null`, dus "geen mening", nooit een gok.
- **Bronlagen.** `JAARGANGEN.md` legt vast welke soort bron mag beslissen. Promotie-organisaties van een land of streek (Wines of Chile, WoSA, Wine Australia) en consejos zijn laag C: bruikbaar voor feiten (opbrengst, vorst, ziektedruk, oogstdata), nooit de beslissende stem over kwaliteit, want ze verklaren hun eigen jaargang bijna nooit slecht. Gemeten bewijs: Ribera del Duero gaf in veertig jaar nooit een onvoldoende. Handelaren zijn laag B (hun lage oordelen wegen zwaar, hun hoge minder), critici met terugkijkende proeverijen laag A. Verder: een terugkijkend oordeel gaat voor en-primeur, en of een producent declareerde of declasseerde is het eerlijkste signaal dat er is.
- **Gecontroleerd of geschat.** Een plus achter het niveau (`2016:5+`) betekent: tegen minstens twee onafhankelijke gepubliceerde bronnen gelegd, met de vindplaats in `JAARGANGEN.md`. Zonder plus is het eigen schatting. `JAARBRON` houdt dat bij, `jaargangOordeel()` geeft het mee als `bron`, en de app toont het (vraagteken achter het woord, voluit in de vensteruitleg). Stand na ronde twee (16 sep 2026): 246 van 818 jaren gecontroleerd; 21 van de 38 streken nog helemaal niet. Ronde twee deed de Douro (0 naar 17, met 1997 van 5 naar 3 en 2022 van 5 naar 3, en 2024 nieuw op 5) en kwam niet verder, omdat laag B volledig onbereikbaar is en van laag A alleen Vinous leesbaar, zodat de tweede onafhankelijke bron ontbreekt. **Lees pagina's met `curl` vanuit Bash, niet met WebFetch:** die laatste is voor élk domein geblokkeerd, ook voor hosts die met curl gewoon opengaan, en curl kost geen zoekbudget. **Nooit een plus zetten zonder de bron in `JAARGANGEN.md` te noteren.** De werklijst met wat als eerste moet worden nagetrokken staat daar ook, met Sauternes 1983 en de Douro-declaraties bovenaan.
- **Een ronde invoeren** gaat via `tools/zet-jaargangen.py` (droogloop standaard, `--schrijf` om door te voeren). Nooit met de hand overtypen: het script controleert de vorm en drukt de diff per streek af, zodat de ronde na te rekenen is.
- **Waarom een eigen tabel.** Punten van Parker of Decanter blijven verboden (zie de ontwerpregels bij "Niveau van een fles"): die hangen aan één fles, zijn redactioneel eigendom, en een taalmodel verzint ze overtuigend. Een jaargangreputatie per streek is openbare kennis en past in één woord. Bijwerken: elk najaar het nieuwe oogstjaar erbij en de jonge jaren bijstellen. De tests toetsen de vorm van de tabel (geldige jaren, niveaus 1 tot 5, genormaliseerde trefwoorden, geen dubbele sleutels), niet de inhoud.
- **Bewaarduren.** `vensterBasis(w, ...)` geeft `{van, tot, soort}` in jaren na de jaargang, `estimateWindow` legt de jaargangfactor eroverheen (`JAAR_FACTOR`, `JAAR_START`) met een dak van 60 jaar (madeira 150). `soort` is de zin die de app aan de gebruiker toont. Lijsten om op te letten: `PRESTIGE` (champagnes waar huizen decennia voor rekenen), `EDELZOET`, `VROEG` (nouveau, vinho verde, moscato: die krijgen nooit een lang venster, wat de druif ook zegt). Een fles zonder jaargang hangt aan `vensterAnker(w)`, het jaar dat hij in de kelder kwam, niet aan vandaag.
- **Standen.** `windowStatus` geeft `onb | jong | op | nu | rijp | over`. `rijp` ("Op z'n rijpst") is nieuw en zit tussen het venstereinde en `vensterUitloop`, die het maximum neemt van 35 procent van de spanwijdte en wat `estimateWindow` er nog bij denkt (dak 40 jaar). Zo vangt een te kort venster uit een CSV of van de AI zichzelf op. "Drink binnenkort" heeft een dak van drie jaar. Bij een nieuwe stand: `URG`, `URGENTIE`, de filters (`UI.fstatus`, `UI.fovertijd`), het dashboard, `cellarLines`, `serveAdvice`, `verdict`, de badge-CSS (`.b-rijp`) en de Edge Function `herinnering` allemaal mee.
- **Herkomst.** `drinkSrc` is `regels | ai | bestand | eigen`, genormaliseerd in `schoonWijn`. `vensterBron(w)` valt voor oude flessen zonder veld terug op een vergelijking met wat de regels nu zeggen, en zegt liever niets dan een gok. `vensterHtml(w)` en `ACT.vensterUit` tonen het oordeel en de hele onderbouwing, inclusief de grenzen (per streek, niet per producent; hoe de fles lag weegt zwaarder). Dat blok is het antwoord op "waar haalt hij die drinkbaarheid vandaan", en dat is de vraag waarop gebruikers de app vertrouwen of niet.
- **Migratie.** `vensterMigratie()` herrekent alleen vensters die exact gelijk zijn aan `oudVenster(w)`, de bevroren oude formule. Idempotent zonder vlag: wat langsgaat krijgt `drinkSrc:'regels'`. Aangeroepen bij het opstarten, in `adoptDoc`, bij het terugzetten van een reservekopie en in `doImport`. `oudVenster` nooit ergens anders gebruiken.
- **AI.** `RIJPHEIDSREGELS` staat in de scanprompt en in `aiRecalc`, met de ordes van grootte per categorie, omdat een model uit zichzelf het voorzichtige antwoord van een winkelier geeft. `jaargangPrompt(w)` geeft het tabeloordeel mee waar we de streek al kennen. `ensureWindow` legt het antwoord daarna nog één keer tegen de regels aan: vijf jaar of meer verschil aan de achterkant en de regels winnen (`VERZOEN_JAAR`).
- **Prijs van een andere jaargang.** `jaargangKloof(w)` zegt bij `valueBron.vintage !== w.vintage` hoe groot het gat is, welke kant het op wijst en wat de jaargangtabel van beide jaren vindt. **Rekent het prijsverschil niet uit, en dat is met opzet:** `wine_prices` had op 16 sep 2026 geen enkele wijn met twee jaargangen, dus elke vermenigvuldiger zou verzonnen zijn. Meting van die 42 rijen staat in `DECISIONS.md`: 4 rijen op een andere jaargang, 14 met een platte bandbreedte, 6 met een buitenlandse markt, en advertentiesites en promotiefolders tussen de bronnen. Een platte of nulband wordt nu de eerlijke marge rond het bedrag. Open: een strengere bronpoort in de Edge Function en markt-geografie per rij.
- **Voorbeeldkelder.** `ACT.demoLoad` rekent de vensters zelf uit met `estimateWindow`, dus de voorbeeldflessen laten altijd zien wat de app echt doet.

## Schrijfstijl
Geen gedachtestreepjes en geen "niet X, maar Y". Geldt voor de app, voor dit bestand en voor antwoorden in de chat. `STIJLREGELS` gaat via `styleNote()` mee in élke AI-prompt, anders komt het via de gegenereerde tekst weer binnen. Een los `—` als "geen waarde" in een tabelcel en getalbereiken zoals `16–18 °C` blijven.

## Nog open
- Stripe live vóór er vreemden bij komen; uitgavenplafond bij Anthropic. Alles op naam van Ponsen Polutropon B.V.: Stripe-onboarding (account `acct_1UAm7oL0f2iIgWz0` staat nog op "onboarding niet af") met KvK 77838335, en de Anthropic-facturen naar de BV. Supabase staat sinds 15 sep op Pro op naam van de BV (zie Status & vervolg).
- Vestigingsadres (Westerstraat 244, 1015 MT Amsterdam) staat sinds 6 sep in `AANBIEDER_ADRES`. Moneybird hoort bij Atelier Marx (bedrijf van Max' vrouw) en staat los van de BV; nooit voor CellarMentor gebruiken.
- Kostprijs per credit meten met `ai_usage` (`tokens_in`/`tokens_out`). Blijft het onder $0,010, dan klopt de bundel van 300 voor €2,99.
- Positionering: CellarMentor beantwoordt "welke fles moet vanavond open", Vivino "moet ik deze kopen". Echte concurrent is CellarTracker, niet Vivino.

### Niveau van een fles: gebouwd als "Plek in je kelder" (2 sep 2026)
Gebouwd volgens de regels hieronder. `plekInKelder(w)` rekent relatief aan de eigen kelder (minstens zes wijnen met een prijs; top = bovenste 10% of drie stuks én ≥1,5× de mediaan; "duurder" = bovenste kwart én ≥1,25× de mediaan; daaronder stilte). `verdict(w)` is de ene zin "bewaren of openen", zonder gok naar de gelegenheid. `klasse(w)` herkent classificaties in `appellation`/naam (`KLASSEN`). Prijzen dragen een bron: `valueSrc` is `ai` (schatting), `zoek` (zoekagent, met `valueBron` {name,url,vintage}) of `eigen` (zelf ingevuld); `valueAt` is de datum. "Je topfles" alleen bij een prijs met bron, anders "waarschijnlijk". Kaart: ◆ (bron) of ◇ (schatting) bij topflessen. Scoreveld `score` (vrije tekst, sorteerbaar); de scanner vult `score_seen` alleen als het letterlijk op de foto staat.

**Zoekagent (was kind `prijs` voor 5 credits; sinds 6 sep is dat `prijsdiep`, en `prijs` is de Brave-laag voor 1 credit, zie "Prijzen met bron"):** `aiPrijs(w)` stuurt `web:true` + `wine` mee; de Edge Function zet de server-side `web_search_20260209` erbij en kijkt eerst in `wine_prices` (SQL in `supabase/sql/wine_prices.sql`, onbeperkt geldig met datum ("gegevens van sep 2026"); ouder dan een jaar toont de app "Opnieuw opzoeken" en stuurt `refresh:true`, wat de tabel overslaat; cache-hit = gratis en geen `ai_usage`-rij; het letterlijke modelantwoord staat per zoekopdracht in `wine_price_log`). Knop "Prijs opzoeken" in het detail, bulkknop in Instellingen (`prijsBulk`: ouder dan acht jaar of aan de bovenkant), na een scan nooit ongevraagd: bij een tabel-miss een toast met één tik "Zoek op · 5 credits" (`prijsNaScan`). De zoekagent draait op Haiku 4.5 (`MODEL_BY_KIND.prijs`) met de basiszoekfunctie `web_search_20250305`, hoogstens twee zoekrondes en een vaste sitelijst (`PRIJS_SITES`); Haiku krijgt geen `thinking`-veld. Eerste geslaagde Haiku-zoekopdracht (2 sep, Quarts de Chaume 2014): €58 via iDealwine, jaargang 2011, 31k tokens in, ± $0,06. Les: een klein model heeft genummerde regels nodig, anders geeft het null zodra de exacte jaargang ontbreekt. De prompt vraagt sinds v46 ook de flesmaat (`size_seen`) en omrekening naar 75 cl, omdat zoete wijnen vaak per 50 cl gaan. De prijsprompts vragen sinds 2 sep om de marktwaarde van díe jaargang nu, met brede band bij oude wijnen. Eerste echte aanroep gemeten (2 sep): 43.768 invoertokens, 1.477 uit, ± $0,14 per fles; daarom 5 credits en hoogstens drie zoekopdrachten. Gratis tabelpad: `kind:'prijscache'` met `wines[]` (geen AI, geen credit); de app vraagt het na elke scan (`prijsNaScan`) en één keer per dag voor de hele kelder (`prijsTabelRonde`, `S.settings.prijsRondeOp`). Gemeten op Max' kelder: jonge en bekende wijnen kloppen, oude lopen beide kanten op (Quarts de Chaume 2014 2,5× te laag, Anthonic 2010 te hoog), vandaar de agent.

### De ontwerpregels (blijven gelden)
Mensen krijgen wijn cadeau en weten niet of het een fles voor vanavond is of een om te bewaren. Vivino lost dat op met community-scores; die database hebben wij niet en gaan we niet inhalen. De vraag is dus: hoe laat je niveau zien zonder reviewdatabase?

Wat we al hebben of gratis kunnen krijgen: de geschatte waarde (`value_eur` met bandbreedte), de classificatie in `appellation` (Grand Cru Classé, DOCG, Riserva, VDP Grosse Lage; openbare structuurkennis waar een taalmodel betrouwbaar in is), en de reputatie van producent en jaargang (kwalitatief, zonder cijfer).

Twee harde randvoorwaarden, allebei uit een fout die al gemaakt is:
1. **Relatief aan de eigen kelder, nooit absolute prijsklassen.** Veel mensen hebben alles tussen €5 en €20. Voor hen is die fles van €19 de bijzondere. Een vaste schaal noemt die "doordeweeks" en heeft het mis. "Een van je duurdere flessen" klopt bij elke kelder.
2. **Nooit neerbuigend over een fles die iemand bezit of cadeau kreeg.** Zeg waar een wijn goed tot z'n recht komt, niet waar hij te min voor is. "Prima bij het koken" is precies de zin die niet moet.

Over externe scores (Parker, Hamersma, Decanter): **nooit uit het geheugen van de AI laten komen.** Die getallen worden overtuigend verzonnen, ze hangen per jaargang, en het is redactioneel eigendom van anderen. Wel goed: een vrij veld waarin de gebruiker zelf noteert wat hij gezien heeft ("92 Parker", "8,5 Hamersma"), sorteerbaar; en de scanner mag een score overnemen die echt op de foto staat (schapkaartje, halslabel), met de bron erbij.

Wat we wél hebben en Vivino niet: het oordeel van de gebruiker zelf over flessen die hij echt heeft opgedronken (sterren in de historie, smaakprofiel). Aggregeren over gebruikers heen raakt de privacyverklaring, die nu zegt dat we niets delen. Iets voor bij schaal, niet nu.
- Wens-backlog: gedeelde kelder voor twee mensen, échte pushmeldingen (vergt berichtenserver), meertalige versie.
- **Al geproefd bij toevoegen (2 sep).** Scan-bevestiging en handmatig toevoegen tonen `proefHtml()` (sterren `#proefStars`, plek, notitie); `proefLees()` vóór het sluiten, `proefSchrijf(w, pr)` maakt een historie-item met `proef:true` zonder een fles af te boeken. Telt meteen mee in `smaakGeleerd()`. In de historie herkenbaar aan "🍇 geproefd bij aankoop".
- **Smaakprofiel (gebouwd 2 sep).** Meer → Jouw smaak (`viewSmaak`): druiven, streken, vijf schuifjes (midden = geen voorkeur), kleur; opgeslagen in `S.settings.smaak`. `smaakGeleerd()` leert uit sterren in de historie (vanaf vijf beoordelingen; druif hoog/laag bij n≥2, streek of type laag pas bij n≥3); `drinkSave` bewaart sinds nu druiven en streek in het historie-item. `smaakNote()`/`smaakPrompt()` gaan mee in `aiDish`, `aiMenu` en `aiAsk`; `smaakMatch(w)` (+1/0/−1) telt 0,8 punt mee in `scoreWines` en toont "♥ Past bij je smaak" bij Vanavond, op de wijnkaart en na een scan.
- **Rek met vakjes (gebouwd 3 sep 2026).** Per locatie, niet als algemene instelling: in Meer → Locaties & rekken opent het rasterknopje (`REK_ICOON`, `openRek`) de instelling rijen × kolommen (max `REK_MAX_RIJEN` 26 en `REK_MAX_KOL` 40) of, als het rek er al is, het raster met alle flessen (kleur per type, tik opent het detail). Het rek bezit de kaart: `S.locations[i].rek = {rijen, kolommen, vakken:{"B3": wineId}}`, één fles per vak, de wijn zelf weet er niets van. Vakken heten A1 (linksboven) tot Z99 (`VAK_RE`, `vakGeldig`). In het formulier (`formFields(w, {rek:true})`, alleen bij toevoegen, bewerken en de scanbevestiging, niet bij de bulkimport) verschijnt onder Locatie het raster in de stand `kies` zodra de gekozen locatie een rek is; keuze staat in `formVakken`/`formVakLoc`/`formVakWine`, `rekNaBewaren(w, oudeLoc)` zet het bij het bewaren in het rek en ruimt het oude rek op bij een verhuizing. Meer vakken tikken dan flessen verhoogt bij een nieuwe fles het aantal (`vakErbij`). Afboeken (`drinkSave`, `qtyMinus`) geeft de laatste vakken vrij via `rekBijQty`, ongedaan maken zet ze terug (`rekHerstel`); verwijderen maakt alles vrij (`rekVrij(id, Infinity)`). Kaart en detail tonen "Wijnrek · B3, B4" (`vakKort`); het drinkvenster zegt welk vak je moet pakken. `schoonLoc()` normaliseert locaties bij cloud-pull en back-up; back-ups bevatten sinds nu ook `locations` (met hun foto's). Bewust nog niet: bakken met meer dan één fles per vak (zou een capaciteit per vak worden).
- **Naam.** De app heette tot 15 sep 2026 Caveau. Caveau botst met een EU-beeldmerk uit 2025 (Caveau Ltd, o.a. klasse 9, 33 en 42, een lifestyle- en swingersclubmerk) en caveau.nl/.com zijn bezet; hernoemen vóór domein en advertenties. Brief: een chic woord, herkenbaar in NL/FR/EN/IT, dat "eigen kelder" en "persoonlijke sommelier" uitdrukt. Afgevallen: Sommelo (één letter van de app Sommelio), Vintro, Cuvéo, Cellaro, Apogée, Maturo, Caviste, Sommo. Vrij qua domeinen en zoekmachines: Vinsavant, Maitrecave, Vinmoment, Vindow (.com bezet). **Ronde 2 (3 sep 2026, met TMview):** favoriet **Vintempo** (vin + tempo; register schoon op een verlopen Frans merk uit 2006 na, .app/.nl/.fr/.eu vrij, .com te koop ± $3.900; laten toetsen naast Vinotemp en Vintime), terugval Vinmoment (alles vrij) en Vinzenit. Geblokkeerd gebleken: À Point (FR kl 9/42, 2025), Annata (IS kl 9/42), Decanto (Squidbay, EU kl 9), Custode (IT kl 9), Cavea, Tastevin. Daarna handles nagekeken: @vintempo en @vinmoment zijn op Instagram bezet; **Vintempus** (vin + Latijn tempus) is overal vrij (register, .com/.app/.nl/.fr/.eu, Instagram, TikTok, X) en is de nieuwe favoriet; let op Vitempus (ES-wijnkasten, kl 11). Terugval Vinzenit en Vinsommet, ook overal vrij. Rapport: ~/Downloads/naamonderzoek-wijnkelder-app-ronde2.md. **Keuze van Max (15 sep 2026): de app heet CellarMentor.** Controle 14 en 15 sep 2026: TMview/EUIPO exact en fuzzy 0 treffers; losse merken MENTOR (Bucci, Kudela) en CellarX (ES, kl 9/35/42, jul 2026) zijn de dichtstbijzijnde, geen blokkade maar CellarX laten toetsen bij de aanvraag; domeinen .com/.app/.nl/.eu/.io/.wine vrij (cellarmentor.com is sindsdien van Max); Instagram geclaimd, YouTube/TikTok/X/GitHub/LinkedIn/Bluesky vrij. Nog te doen: EU-woordmerk aanvragen in kl 9, 35, 42. TMview is te bevragen via POST tmdn.org/tmview/api/search/results met {page,pageSize,criteria:'C',basicSearch}.
- **Brave Search als goedkope zoeklaag (gebouwd 6 sep, zie "Prijzen met bron").** Losse zoek-API ($5 per duizend, $5 credits per maand, creditcard verplicht, geen plafond) plus Haiku op de fragmenten: ± $0,007 per wijn tegen ± $0,06 nu; zou "precies opzoeken" op één credit brengen en voorvullen van 500 wijnen ± $3,50 maken. Vergt secret `BRAVE_SEARCH_KEY`, een harde limiet van één zoekopdracht per wijn, en één zin in de privacyverklaring. Max wil er nog even mee wachten.
- **Verhuizing naar eigen domein** (plan staat, wacht op de naam): nieuwe repo voor de app, het oude adres wordt een brug (sync als verhuiswagen, overdracht via URL-fragment zonder foto's als noodweg, laatste service worker die zichzelf afmeldt), Supabase-auth-URL's bijwerken.
